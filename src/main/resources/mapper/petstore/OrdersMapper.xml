<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csu.petstorepro.petstore.mapper.OrdersMapper">

    <select id="getOrderByOrderId" parameterType="int" resultType="orders">
        select
          billaddr1 as billaddress1,
          billaddr2 as billaddress2,
          billcity,
          billcountry,
          billstate,
          billtofirstname,
          billtolastname,
          billzip,
          shipaddr1 as shipaddress1,
          shipaddr2 as shipaddress2,
          shipcity,
          shipcountry,
          shipstate,
          shiptofirstname,
          shiptolastname,
          shipzip,
          cardtype,
          courier,
          creditcard,
          exprdate as expirydate,
          locale,
          orderdate,
          orders.orderid,
          totalprice,
          userid as username,
          status
        from orders, orderstatus
        where orders.orderid = #{value} and orders.orderid = orderstatus.orderid
    </select>

    <select id="getOrdersByUserId" parameterType="string" resultType="orders">
        select
          billaddr1 as billAddress1,
          billaddr2 as billAddress2,
          billcity,
          billcountry,
          billstate,
          billtofirstname,
          billtolastname,
          billzip,
          shipaddr1 as shipAddress1,
          shipaddr2 as shipAddress2,
          shipcity,
          shipcountry,
          shipstate,
          shiptofirstname,
          shiptolastname,
          shipzip,
          cardtype,
          courier,
          creditcard,
          exprdate as expiryDate,
          locale,
          orderdate,
          orders.orderid,
          totalprice,
          userid as username,
          status
        from orders, orderstatus
        where orders.userid = #{value}
          and orders.orderid = orderstatus.orderid
        order by orderdate
    </select>

</mapper>
